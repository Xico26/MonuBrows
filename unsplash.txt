/* async function findImages() {
        try {
            const images = await axios.get("https://api.unsplash.com/search/photos", {
                params: {
                    client_id: "hello",
                    query: monument.name,
                    page: 1,
                    per_page: 5
                },

            })
            console.log(images)
            /*
            const img0 = await images.data.results[0].urls.small;
            const img1 = await images.data.results[1].urls.small;
            const img2 = await images.data.results[2].urls.small;
            const img3 = await images.data.results[3].urls.small;
            const img4 = await images.data.results[4].urls.small;
            
            // for (let i = 0; i < 5; i++) {
            //     const urls = await images.data.results[i].urls.small;
            //     console.log(urls)
            // }
            res.render("monuments/details", { monument , img0, img1, img2, img3, img4});

        }
        catch (e) {
            next(e)
        }
        
        
    }
    findImages()
    */

    CLIENT_ID=agVqwND_LGafaz9PmRNp7gcg4RPtpcA18zvAOKRvGXM